//creating the event for kafka as the proto we can also do this using the rest also ..
syntax ="proto3";

package patient.events;
option java_multiple_files =true;

message PatientEvent {
  string patientId =1;
  string name =2;
  string email =3;
  string event_type =4;
}