### generate the token for the valid user
POST http://localhost:7004/auth/login
Content-Type: application/json

{
  "email": "testuser@test.com",
  "password" : "password123"
}

> {% client.global.set("token", response.body.token) %}

### above we are getting the auth token from the response (below programatically) and saving it

